# source
source "https://rubygems.org"
# source "https://gems.ruby-china.com"

# bundle install
# bundle exec jekyll serve

# if only jekyll
gem "jekyll"
# else for gitub-pages
# gem "github-pages", group: :jekyll_plugins
# upgrade, run `bundle update github-pages`.

# plugins
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-archives"
  gem "jekyll-seo-tag"
  gem "webrick"
end
